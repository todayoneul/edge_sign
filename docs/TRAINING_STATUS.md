# 학습 상태 / 핸드오프 노트

> **이 문서는 진행 중·완료·중단된 학습의 "현재 진실"을 기록합니다.**
> 다운스트림 작업(ONNX 내보내기, 양자화, 평가, 데모)을 시작하는 모든 작업자/서브에이전트는
> **착수 전 이 파일을 먼저 읽으세요.** 어떤 체크포인트를 쓸지, 왜 그 상태인지가 여기 있습니다.

---

## 🟠 edge_sign_v3_lights (YOLOv8s 검출기, 신호등 분리 v3) — **조기종료(EARLY-STOPPED)**

- **갱신 시각**: 2026-05-31 00:27 (KST)
- **런 디렉토리**: `runs/detect/edge_sign_v3_lights-4/`
- **설정**: YOLOv8s · imgsz 1280 · batch 8 · 계획 40 epoch · `close_mosaic=10` · `patience=20`
- **중단 시점**: ep34/40 완료 후 프로세스 종료 (ep35 진행 중 종료, PID 62456 kill)

### ✅ 채택 체크포인트 (다운스트림은 이것을 사용)
- **`runs/detect/edge_sign_v3_lights-4/weights/best.pt`**
- **best = epoch 29**, mAP50-95 = **0.4457**, mAP50 = **0.7761**
- `last.pt`(ep34)는 best보다 성능이 낮으므로 사용 금지.

### 🔎 조기종료 사유 (왜 멈췄는가)
1. best가 **ep29**에서 나온 뒤 **5 epoch 연속 하락** (mAP50-95: 0.4457→0.4422→0.4407→0.4398→0.4373→0.4330).
2. `close_mosaic=10`로 **ep31부터 mosaic 증강이 꺼진** 구간(ep31~34)에서도 **회복 없이 단조 하락** → 일시적 증강전환 아티팩트가 아니라 **실제 정체/과적합 시작**으로 확정.
3. `patience=20`은 40 epoch 안에서 자동 발동 불가(best 이후 20 epoch 정체 필요). 남은 6 epoch가 ep29를 넘길 가능성 희박.
4. 사용자 지침("발전이 없으면 조기종료")에 따라 ep34에서 재판단 후 종료. **약 70분(6 epoch) 절약**. best.pt는 ep29 시점에 이미 안전 저장됨.

### ➡️ 다운스트림 작업자/서브에이전트가 해야 할 일
- v3 검출기 관련 작업(ONNX export, INT8 양자화, E2E 평가)은 **위 best.pt(ep29)** 기준으로 진행.
- "40 epoch 완주본"을 기다리거나 가정하지 말 것 — **40 epoch는 존재하지 않음.**
- 더 높은 mAP가 필요하면 재학습이 필요하나, 현 데이터/설정에서는 ep29가 사실상 수렴점.

---

## 다른 학습 (참고)
- 활성 학습 프로세스 없음 (2026-05-31 00:27 기준).
- `uvicorn src.pipeline.app` (데모 서버), `pytest`는 별개 프로세스로 잔존.
